'use strict';

var React = require('react-native');
var { AppRegistry, } = React;

var DrawerLayoutMain = require('./DrawerLayoutMain');

AppRegistry.registerComponent('DrawerLayoutMain', () => DrawerLayoutMain);